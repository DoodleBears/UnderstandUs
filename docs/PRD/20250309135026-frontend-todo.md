# Frontend TODO List

## 第一阶段：音频采集基础设施（1天）

### 1. 音频状态管理

- [ ] 创建 `src/lib/audio/types.ts`
  ```typescript
  export interface AudioState {
    isEnabled: boolean
    isMuted: boolean
    deviceId: string | null
    stream: MediaStream | null
    error: Error | null
  }
  ```

### 2. 音频上下文和 Hook

- [ ] 创建 `src/components/audio/AudioProvider.tsx`
  - 实现 AudioContext
  - 管理音频状态
  - 提供音频控制方法
- [ ] 创建 `src/components/audio/useAudio.ts`
  - 封装音频操作方法
  - 提供状态管理接口
  - 实现错误处理

### 3. 设备管理

- [ ] 创建 `src/lib/audio/permissions.ts`
  - 实现设备权限请求
  - 添加权限状态管理
  - 处理权限错误情况
- [ ] 创建 `src/lib/audio/devices.ts`
  - 获取默认设备
  - 处理设备变更

### 4. 音频采集核心

- [ ] 创建 `src/lib/audio/capture.ts`
  - 实现 MediaStream 获取
  - 配置音频参数
  - 实现基础错误处理

## 第二阶段：WebRTC 集成（1天）

### 1. WebRTC 类型定义

- [ ] 创建 `src/lib/webrtc/types.ts`
  ```typescript
  export interface PeerConnection {
    id: string
    connection: RTCPeerConnection
    stream: MediaStream | null
  }
  ```

### 2. WebRTC 连接管理

- [ ] 创建 `src/lib/webrtc/connection.ts`
  - 实现 RTCPeerConnection 创建
  - 处理 ICE 候选
  - 管理连接状态

### 3. 信令集成

- [ ] 创建 `src/lib/webrtc/signaling.ts`
  - 集成现有 WebSocket 连接
  - 实现信令消息处理
  - 处理连接协商

### 4. 音频流管理

- [ ] 创建 `src/lib/webrtc/stream.ts`
  - 实现音频流传输
  - 处理远程音频流
  - 实现基础流控制

## 第三阶段：UI 组件开发（1天）

### 1. 音频控制组件

- [ ] 创建 `src/components/audio/AudioControl.tsx`
  ```typescript
  export interface AudioControlProps {
    className?: string
    onToggleMute: () => void
    onToggleEnable: () => void
  }
  ```
  - 实现麦克风开关按钮
  - 实现静音按钮
  - 添加基础状态显示

### 2. 状态显示组件

- [ ] 创建 `src/components/audio/AudioStatus.tsx`
  - 显示设备状态
  - 显示连接状态
  - 显示错误信息

### 3. 房间集成

- [ ] 更新 `src/components/room/Room.tsx`
  - 集成音频控制组件
  - 添加参与者音频状态
  - 实现多人音频管理

## 第四阶段：测试和调试

### 1. 单元测试

- [ ] 创建 `src/lib/audio/__tests__/capture.test.ts`
- [ ] 创建 `src/lib/webrtc/__tests__/connection.test.ts`
- [ ] 创建 `src/components/audio/__tests__/AudioControl.test.tsx`

### 2. 集成测试

- [ ] 创建 `cypress/e2e/audio.cy.ts`
  - 测试音频权限流程
  - 测试音频控制功能
  - 测试多人连接场景

### 3. 性能监控

- [ ] 创建 `src/lib/audio/metrics.ts`
  - 监控音频延迟
  - 监控 CPU 使用率
  - 记录错误日志

## 依赖安装

```bash
pnpm add @types/webrtc
pnpm add -D jest @types/jest @testing-library/react @testing-library/jest-dom
```
