# Backend TODO List

## 第一阶段：信令服务器基础设施（1天）

### 1. WebSocket 信令服务

- [ ] 创建 `app/websocket/signaling.py`

  ```python
  class SignalingNamespace:
      async def on_ice_candidate(self, sid: str, data: dict):
          pass

      async def on_offer(self, sid: str, data: dict):
          pass

      async def on_answer(self, sid: str, data: dict):
          pass
  ```

### 2. 房间音频状态管理

- [ ] 创建 `app/models/audio_state.py`
  ```python
  class AudioParticipant:
      id: str
      room_id: str
      is_muted: bool
      is_enabled: bool
      peer_connections: List[str]
  ```

### 3. 连接管理

- [ ] 创建 `app/services/connection_manager.py`
  - 管理 WebRTC 连接状态
  - 处理连接断开
  - 实现重连机制

## 第二阶段：WebRTC 信令处理（1天）

### 1. ICE 处理

- [ ] 创建 `app/services/ice_handler.py`
  - 处理 ICE 候选
  - 转发 ICE 消息
  - 管理 ICE 状态

### 2. SDP 处理

- [ ] 创建 `app/services/sdp_handler.py`
  - 处理 Offer/Answer
  - 验证 SDP 格式
  - 转发 SDP 消息

### 3. 连接协商

- [ ] 创建 `app/services/negotiation.py`
  - 实现连接协商流程
  - 处理重协商
  - 管理连接状态

## 第三阶段：房间音频管理（1天）

### 1. 房间音频状态

- [ ] 创建 `app/services/room_audio.py`
  - 管理房间音频状态
  - 处理参与者音频变更
  - 广播状态更新

### 2. 参与者管理

- [ ] 创建 `app/services/participant_manager.py`
  - 管理参与者音频状态
  - 处理参与者加入/离开
  - 维护连接关系

### 3. 事件广播

- [ ] 创建 `app/services/event_broadcaster.py`
  - 广播音频状态变更
  - 广播连接状态
  - 广播错误信息

## 第四阶段：测试和监控

### 1. 单元测试

- [ ] 创建 `tests/test_signaling.py`
- [ ] 创建 `tests/test_room_audio.py`
- [ ] 创建 `tests/test_connection_manager.py`

### 2. 集成测试

- [ ] 创建 `tests/integration/test_webrtc.py`
  - 测试完整信令流程
  - 测试多人连接场景
  - 测试异常处理

### 3. 性能监控

- [ ] 创建 `app/monitoring/metrics.py`
  - 监控连接数
  - 监控消息延迟
  - 记录错误日志

## 依赖安装

```bash
pip install pytest pytest-asyncio pytest-cov
```

## WebSocket 消息类型定义

```python
# app/schemas/signaling.py
from pydantic import BaseModel

class IceCandidate(BaseModel):
    candidate: str
    sdpMid: str
    sdpMLineIndex: int

class SessionDescription(BaseModel):
    type: str  # "offer" | "answer"
    sdp: str

class AudioStateUpdate(BaseModel):
    participant_id: str
    is_muted: bool
    is_enabled: bool
```

## 配置更新

### 1. 环境变量

```env
STUN_SERVERS=stun:stun.l.google.com:19302
TURN_SERVERS=
MAX_PARTICIPANTS_PER_ROOM=10
```

### 2. 应用配置

```python
# app/config.py
WEBRTC_CONFIG = {
    "iceServers": [
        {"urls": STUN_SERVERS.split(",")}
    ]
}
```
