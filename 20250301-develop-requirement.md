现在请帮我构建一个简单的 MVP 产品系统，之后将会用于实际的用户测试，所以需要部署到线上

1. 需要具有简单的线上会议功能（简化的 Discord，Zoom）（考虑到可以线下举行实验，也可以线上举行实验）
2. 用户的使用方式是：假设有两台电脑，要求双方同时打开提供服务的网站，并输入房间号（加入到同一房间）
   1. 进入之后两个人可以开始实时对话
3. 系统需要实时处理音频文件，转换为 Transcript
4. 在用户进入房间后的，显示的前端中，需要
   1. 实时显示用于语音转录出来的文本 Transcript
   2. 同时显示 LLM 生成的内容
      1. 此处生成的方式是，调用 LLM 的 API
      2. 将 Transcript 作为 Context 上下文，然语言模型根据当前对话，生成对某一句话的理解（揣测是否当前对话存在不同的理解方式可能会导致误解）
5. 整体来说
   1. 数据处理
      1. 处理音频数据，录音
      2. 转换音频为文本数据，语音转文字
   2. 前端交互显示
      1. 显示转录的文本数据
      2. 将 LLM 生成的「对一句话的多个理解」的 JSON 数据，解析为一道多项选择题
         1. 显示对应 message
         2. 在 message 的下方显示选择题选项

---

以上是系统的设计，在开发层面

1. 前端指定使用 React
   1. 请推荐开源免费的热门组件库
2. 后端使用 Python
3. 使用 Docker 来启用后端和前端的服务，同时用于 infrastructure 的部署
4. 云服务请使用尽可能方便的服务提供商
   1. Vercel、AWS
   2. 需要提供商支持实时音频对话模式，最好有方便的木块
